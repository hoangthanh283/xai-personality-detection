Map each behavioral quote to a psychological state. Return ONLY a JSON array.

Evidence:
{% for ev in evidence_list %}{{ loop.index }}. [{{ ev.behavior_type }}] "{{ ev.quote }}"
{% endfor %}

Output (JSON array only, no explanation):
[{"evidence_idx": 1, "quote": "...", "state_label": "...", "confidence": 0.8, "reasoning": "..."}]
